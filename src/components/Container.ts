import { styled } from "@src/styles/system-styled";

const Container = styled("div", {
  maxWidth: "78rem",
  margin: "0 auto",
  marginTop: "$16",
  padding: "0 $6",
  color:"Black"
});

export default Container;
